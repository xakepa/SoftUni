function isExcellent(num) {
    const excellent = 'Excellent'
    const notExc = 'Not excellent'
    if (num >= 5.5) {
        return console.log(excellent);
    } else {
        return console.log(notExc);
    }
}